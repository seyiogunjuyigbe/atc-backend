# Membership Definitions
  Membership:
    type: object
    properties:
      name:
        type: string
        description: membership name
      type:
        type: string
        description: one-off,annual
      description:
        type: string
        description: membership description
