# Package Definitions
  Package:
    type: object
    properties:
      name:
        type: string
        required: true
        description: Name of the package
      length_value:
        type: integer
        description: Package length length_value
      frequency:
        type: string
        description: Packange frequency (days, weeks)