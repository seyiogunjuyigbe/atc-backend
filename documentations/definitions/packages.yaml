# Package Definitions
  Package:
    type: object
    properties:
      name:
        type: string
        required: true
        description: Name of the package
      length:
        type: integer
        description: Package duration (days)